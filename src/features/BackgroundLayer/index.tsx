import styles from './BackgroundLayer.module.css';

function BackgroundLayer() {
  return (
    <section id="background" className={styles.custom_background} />
  );
}

export default BackgroundLayer;
